#numbers = [5, 16, -3, 0, 12]

#positive = 0

#negative = 0

#zero = 0

#for i in numbers:
    #if i > 0:
        #print(f"{i} is positive")
        #positive = positive + 1
    #elif i < 0:
        #print(f"{i} is negative")
        #negative = negative + 1
    #elif i == 0:
        #print(f"{i} is zero")
        #zero = zero + 1

#print(f"Number of positives: {positive}")
#print(f"Number of negatives: {negative}")
#print(f"Number of zeroes: {zero}")



#numbers = [4, 12, -3, 6]

#for i in range(len(numbers)): #len tells the computer to go through the entire length of {numbers}. Makes it easy to add numbers to {numbers} without having to adjust the range
    #print(numbers[i])

#for i in numbers: #does the same thing as line 26. Python shorthand.
    #print(i)